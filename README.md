# book-store

To start the local dev server (with hot reloading), use:

```
yarn start
```

or

```
npm run start
```

Then navigate to `http:localhost:1324`.

To build, use:

```
yarn build
```

or

```
npm run build
```
